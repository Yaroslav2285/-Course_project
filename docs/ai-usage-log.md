| Дата/Время | Задача/Промпт | Файлы | Результат | Верификация | Ручные правки |
|---|---|---|---|---|---|
| 2026-05-26 | Этап 1: структура монорепозитория и инфраструктура | `docker-compose.yml`, `.env.example`, `Makefile`, `.pre-commit-config.yaml`, `.gitignore`, `.editorconfig`, `.gitattributes`, `docs/README.md`, `services/*/.gitkeep`, `infra/.gitkeep`, `scripts/.gitkeep` | Созданы базовая структура и инфраструктурные файлы | `docker compose config && make up && docker ps` и `pre-commit run --all-files` | Убедиться, что `Makefile` и `docker-compose.yml` подходят для следующего этапа с Dockerfile-ами и сервисами |
| 2026-05-26 | Этап 2: модели данных и миграции для Python и Go | `services/python-api/models/users.py`, `services/python-api/models/services.py`, `services/python-api/models/orders.py`, `services/python-api/models/base.py`, `services/python-api/alembic/env.py`, `services/python-api/alembic.ini`, `services/go-escrow/internal/db/migrations/001_initial.sql` | Сгенерированы SQLAlchemy модели, Alembic async конфигурация и Go SQL миграция | `cd services/python-api && alembic revision --autogenerate -m "init" && alembic upgrade head` и `cd services/go-escrow && goose create init sql && goose up` | Проверить, что `alembic.ini` и `env.py` корректно настроены и миграции создаются без ошибок |
| 2026-05-26 | Исправление Alembic конфигурации и добавление requirements.txt | `services/python-api/alembic/env.py`, `services/python-api/requirements.txt`, `docs/ai-usage-log.md` | Добавлено чтение DB_URL из окружения для Alembic и зафиксированы Python зависимости для этапа миграций | `cd services/python-api && python -m pip install -r requirements.txt && python -m alembic -c alembic.ini revision --autogenerate -m "init"` | Проверить, что `DB_URL` доступен в окружении и Alembic запускается из каталога сервиса |
| 2026-05-26 | Исправление Alembic секции в alembic.ini | `services/python-api/alembic.ini`, `docs/ai-usage-log.md` | Перенесён `sqlalchemy.url` в секцию `[alembic]`, чтобы Alembic корректно читал URL | `cd services/python-api && python -m alembic -c alembic.ini revision --autogenerate -m "init"` | Проверить, что Alembic запускается из каталога `services/python-api` и `DB_URL` установлен при необходимости |
| 2026-05-26 | Добавление шаблона Alembic script.py.mako | `services/python-api/alembic/script.py.mako`, `docs/ai-usage-log.md` | Восстановлен отсутствующий шаблон для генерации ревизий Alembic | `cd services/python-api && python -m alembic -c alembic.ini revision --autogenerate -m "init" && python -m alembic -c alembic.ini upgrade head` | Проверить, что `alembic/versions` создаёт файлы ревизий и что `upgrade head` проходит |
| 2026-05-26 | Исправление миграции: DROP TABLE IF EXISTS и удаление неиспользуемого импорта | `services/python-api/alembic/versions/fff36bd858b0_init.py`, `services/python-api/models/base.py`, `docs/ai-usage-log.md` | Заменён `DROP TABLE goose_db_version` на `DROP TABLE IF EXISTS goose_db_version` (падал на свежей БД); удалён неиспользуемый `import enum` из `base.py` | `docker run -d --name pg_test -e POSTGRES_DB=app_db -e POSTGRES_USER=app_user -e POSTGRES_PASSWORD=ChangeMe123! -p 5432:5432 postgres:15-alpine && cd services/python-api && alembic upgrade head && alembic check` | Проверить, что `alembic upgrade head` проходит на чистой БД без ошибок и `alembic check` выводит `No new upgrade operations detected` |
| 2026-05-26 | Этап 3: Python Core (FastAPI, JWT, CRUD, Pydantic V2) | `core/config.py`, `core/db.py`, `core/security.py`, `core/exceptions.py`, `core/deps.py`, `core/responses.py`, `schemas/common.py`, `schemas/users.py`, `schemas/services.py`, `schemas/orders.py`, `repositories/base.py`, `repositories/users.py`, `repositories/services.py`, `repositories/orders.py`, `api/v1/auth.py`, `api/v1/users.py`, `api/v1/services.py`, `api/v1/orders.py`, `main.py`, `Dockerfile`, `requirements.txt`, `tests/` (5 файлов) | 32 файла: JWT auth, 4 CRUD эндпоинта с пагинацией, `condecimal` для цен, единый формат ответа, X-Request-ID, CORS, structlog, 23 теста (91% покрытие) | `cd services/python-api && ruff check . && pytest tests/ -v --cov=.` | Проверить, что Dockerfile собирается (`docker build -t python-api ./services/python-api`), тесты проходят на PostgreSQL (через `docker compose up -d postgres`) |
| 2026-05-26 | Этап 3.5: Web UI + SQLite migration + bugfixes | `core/config.py`, `core/db.py`, `core/exceptions.py`, `main.py`, `api/v1/ui.py`, `templates/base.html`, `templates/index.html`, `templates/login.html`, `templates/dashboard.html`, `templates/escrow_status.html`, `static/css/style.css`, `static/js/app.js`, `requirements.txt`, `tests/conftest.py` | 14 файлов: HTML5/CSS3/JS frontend (5 Jinja2 шаблонов), SQLite вместо PostgreSQL, исправлен TemplateResponse, get_db commit, static 404 через url_for, тесты под SQLite | `ruff check . && python -m pytest -v` | Убедиться, что все 23 теста проходят, сервер стартует на SQLite без .env, статика отдаётся через /api/static/ |
| 2026-05-27 | Этап 4: Go Escrow Microservice (Gin, state-machine, NUMERIC(19,4), rate-limit, idempotency) | `main.go`, `internal/config/config.go`, `internal/db/db.go`, `internal/domain/escrow.go`, `internal/repository/escrow_repo.go`, `internal/service/escrow_service.go`, `internal/api/middleware.go`, `internal/api/idempotency.go`, `internal/api/handler.go`, `internal/api/router.go`, `Dockerfile`, `Makefile`, `.env.example`, `go.mod`, `internal/domain/escrow_test.go`, `internal/service/escrow_service_test.go`, `internal/api/handler_test.go` | 17 файлов: Gin REST API (POST /v1/escrow, /fund, /release, /dispute, GET /{id}, /health), State-machine с 7 статусами и картой валидных переходов, X-Idempotency-Key (in-memory + TTL + goroutine cleanup), token-bucket rate-limiter на /fund, zap JSON logging, graceful shutdown, 47 table-driven тестов | `cd services/go-escrow && go build ./... && gosec ./... && go test ./... -coverprofile=cover.out` | Убедиться, что gosec установлен (0 issues), проверить запуск через docker compose, создать интеграционные тесты для полного escrow-цикла |
| 2026-05-27 | Исправление Этапа 2: Makefile, seed_db.py, healthcheck curl | `Makefile`, `scripts/seed_db.py`, `services/python-api/Dockerfile`, `.dockerignore` | Makefile: удалён дубликат `own:`, test запускает pytest + go test; seed_db.py: полный цикл регистрации(клиент+провайдер) → услуга → публикация → заказ; Dockerfile: добавлен curl для healthcheck; .dockerignore: исключён .env из сборки | `make test-python && make test-go`, `python scripts/seed_db.py` | Проверить `Dockerfile` — curl установлен; `.dockerignore` — .env не копируется |
| 2026-05-27 | Ручная проверка Этапа 3 (Python API через Docker) | — | Проверены: регистрация, логин, users/me, services CRUD, orders CRUD, статусы заказов, error-коды (401/404/409). Все эндпоинты работают с PostgreSQL | `curl http://localhost:8000/health`, `curl -X POST ... /v1/auth/register`, `curl -X POST ... /v1/orders/` | Обратить внимание: services — PUT (не PATCH), users/me — PUT, orders статусы — строгий enum, services листинг — только published по умолчанию |
| 2026-05-27 | Исправление ошибок в Docker-сборке Python API | `requirements.txt`, `main.py`, `services/python-api/Dockerfile` | Добавлен email-validator (ошибка pydantic); убрано условие `if _is_sqlite` для create_all — таблицы создаются для любого движка; добавлен curl в образ для healthcheck | `docker compose up -d --build python-api && docker ps` | Проверить что контейнер healthy, health эндпоинт отвечает |
| 2026-05-27 | Исправление DB_URL в Docker-контейнере | `services/python-api/.dockerignore` | Создан .dockerignore для исключения .env из Docker-образа. Старый контейнер использовал SQLite из .env вместо PostgreSQL из docker-compose | `docker compose up -d --force-recreate python-api && docker exec marketplace_python_api env \| Select-String DB_URL` | Убедиться что DB_URL = `postgresql+asyncpg://...`, а не `sqlite+...` |
| 2026-05-27 | Ручная проверка Этапа 4 (Go escrow через curl) | — | Проверены: POST /v1/escrow (201), /fund (200), /release (200), /dispute (200), GET /{id} (200), invalid transition (409), idempotency (409 cached), rate-limit (429), health (200), invalid UUID (400), not found (404) | `curl http://localhost:8081/health`, `curl -X POST http://localhost:8081/v1/escrow/ ...` | Проверить, что go-escrow не конфликтует с python-api на одном порту |
| 2026-05-27 | Комплексная проверка Этапов 2-4 | — | Подтверждена работоспособность всех этапов: Stage 2 (таблицы в PG, Alembic), Stage 3 (23/23 тестов, Docker healthy), Stage 4 (47/47 тестов) | `pytest -q`, `go test ./...`, `docker ps`, `alembic upgrade head` | — |
| 2026-05-27 | Этап 5: Blockchain Simulator (аудит-трейл, SHA-256, SQLite) | `services/blockchain-sim/requirements.txt`, `services/blockchain-sim/Dockerfile`, `services/blockchain-sim/config.py`, `services/blockchain-sim/schemas.py`, `services/blockchain-sim/blockchain.py`, `services/blockchain-sim/database.py`, `services/blockchain-sim/main.py`, `services/blockchain-sim/tests/__init__.py`, `services/blockchain-sim/tests/conftest.py`, `services/blockchain-sim/tests/test_blockchain.py` | 10 файлов: FastAPI сервис на порту 8082, Block/Blockchain с SHA-256, SQLite persistence, 4 эндпоинта (POST /v1/chain/submit, GET /v1/chain/verify, GET /v1/chain/audit/{order_id}, GET /health), 10 тестов (99% покрытие), structlog, Pydantic V2 валидация, prepared statements | `cd services/blockchain-sim && ruff check . && black --check . && pytest tests/ -v --cov=. && bandit -r .` | Проверить интеграцию: Go escrow вызывает POST /v1/chain/submit после каждого изменения статуса. Проверить docker compose build для blockchain-sim. |
| 2026-05-27 | Этап 6: Integration, Inter-service Clients, and Testing | `docs/contracts/escrow-api.md`, `docs/contracts/blockchain-events.md`, `services/python-api/app/__init__.py`, `services/python-api/app/services/__init__.py`, `services/python-api/app/services/escrow_client.py`, `services/python-api/core/config.py`, `services/python-api/requirements.txt`, `services/python-api/tests/test_escrow_integration.py`, `services/go-escrow/internal/clients/blockchain_client.go`, `services/go-escrow/internal/clients/blockchain_client_test.go`, `services/go-escrow/internal/config/config.go`, `services/go-escrow/internal/service/escrow_service.go`, `services/go-escrow/internal/domain/escrow_test.go`, `services/go-escrow/integration_test.go`, `docker-compose.yml`, `Makefile` | 16 файлов: REST contracts (Python↔Go, Go↔Blockchain), Python async httpx client with retry+idempotency, Go blockchain client with retry queue, wired emitBlockchainEvent into state-machine, 50+ domain tests, 2 E2E integration tests, unified Makefile targets | `pytest -v --cov=. && go test ./... -v -cover && bandit -r . && gosec ./...` | SAST: bandit 1 MEDIUM (0.0.0.0 false-positive), gosec 0 issues (5 suppressed). Python 32/32 (93%), Go all pass. |
| 2026-05-28 | Проверка Этапа 3 (Python API) + фикс conditional UI import и bcrypt downgrade | `services/python-api/main.py`, `docs/ai-usage-log.md` | Сделан conditional import `ui.py` (без jinja2 тесты падали). Понижен bcrypt с 5.0.0 до 4.2.1 (passlib совместимость). 32/32 тестов, 91% coverage, bandit 0 Critical/High. curl: все эндпоинты API проверены. | `pytest tests/ -v --cov=api --cov=core --cov=models --cov=repositories --cov=app && python -m bandit -r .` | 32 passed, 91% coverage. bandit: 1 Low (строка "refresh"), 1 Medium (0.0.0.0 false-positive). |
| 2026-05-28 | Проверка Этапа 4 (Go Escrow) — полный цикл | — | Проверены: 17 Go тестов (все пакеты), gosec 0 issues, curl: health, create, fund, advance, release, dispute, invalid UUID/transition/amount, двойной fund, dispute без reason, X-Request-ID echo, idempotency create+fund | `go test ./... -v -count=1 && gosec ./... && curl ...` | All tests pass. gosec 0 issues (5 #nosec). Idempotency, rate-limiter (rps=100/burst=200), state-machine transitions — всё работает. |
| 2026-05-28 | Проверка Этапа 2 (модели + миграции) | — | Проверены: Python модели (users/services/orders), Alembic миграция fff36bd858b0, Go SQL миграция 001_initial.sql, PG таблицы (7 шт.), ruff, Go build | `pytest -q && go build ./... && docker exec ... psql -c "\dt" && ... -c "\d+ users"` | Все таблицы с правильными типами, FK, NUMERIC(19,4), индексы, CHECK-constraints. |
| 2026-05-28 | Исправление healthcheck go-escrow (distroless + /bin/sh) | `services/go-escrow/Dockerfile`, `services/go-escrow/cmd/healthcheck/main.go`, `docker-compose.yml` | Создан Go healthcheck бинарник (не требует shell). Добавлен в multi-stage build. Убран CMD-SHELL healthcheck из docker-compose.yml (конфликтовал с distroless). Все 5 контейнеров healthy. | `docker compose up -d --build go-escrow && docker ps` | Все 5 контейнеров `healthy`. go-escrow API отвечает. |
| 2026-05-28 | Phase 7.1 — Pure HTML/CSS/JS UI scaffold + auth + role-based redirect | `api/v1/ui.py`, `main.py`, `static/css/main.css`, `static/js/api.js`, `static/js/utils.js`, `static/js/auth.js`, `static/js/app.js`, `templates/base.html`, `templates/index.html`, `templates/login.html`, `templates/register.html`, `templates/dashboard.html`, `templates/dashboard_client.html`, `templates/dashboard_executor.html`, `templates/catalog.html`, `templates/orders.html`, `templates/order_detail.html`, `templates/audit.html` | 18 файлов: чистый HTML5/CSS3/Vanilla JS без CDN/frameworks. CSS-переменные, JWT auth, role-based redirect (client→/dashboard/client, provider→/dashboard/executor), toast/alert/badge утилиты, navbar update, UI routes в ui.py. | `curl localhost:8000/login` (200), `curl localhost:8000/register` (200), `curl localhost:8000/api/static/css/main.css` (200), `curl localhost:8000/api/static/js/api.js` (200) | Проверить регистрацию+логин в браузере, подтвердить редиректы по ролям |
| 2026-05-28 | Phase 7.2 — Catalog with filters & pagination | `templates/catalog.html`, `static/css/catalog.css`, `static/js/catalog.js`, `templates/base.html` | Full catalog page with sidebar (search, price range, sort), service grid, pagination, skeleton loading, role-aware order button | `curl localhost:8000/catalog` (200), `curl .../catalog.css` (200), `curl .../catalog.js` (200), `curl .../v1/services/` (valid JSON) | Order endpoint requires backend with existing services; create test data via API to verify card rendering |
| 2026-05-30 | Fix Python 3.14 pydantic-core incompatibility | `services/python-api/requirements.txt` | Upgraded pydantic to ≥2.11.2 (unpinned), which pulls pydantic-core ≥2.46.x compatible with Python 3.14 | `pip install -r requirements.txt` (success) | Lockfile should be regenerated on production build |
| 2026-05-30 | Add footer to base template | `templates/base.html`, `static/css/main.css` | Added `<footer>` block with copyright to base.html, footer CSS styles with border-top, muted text | Visual check in browser | — |
| 2026-05-30 | Restructure navbar: static HTML + JS enhancement | `templates/base.html`, `static/js/auth.js` | Navbar now has static `<a>` tags as fallback (Login/Register). `updateNavbar()` toggles between guest (Login+Register) and authenticated (Catalog+Dashboard+user info+Logout) states instead of building from scratch. Guest navbar no longer shows Catalog link. | Hard refresh (Ctrl+Shift+R) required to clear browser cache | Only visible links change; navbar DOM structure always present |
| 2026-05-30 | Phase 7.3 — Dashboard & Orders UI | `static/js/dashboard.js`, `templates/dashboard_client.html`, `templates/dashboard_executor.html`, `static/css/main.css` | Client dashboard: orders table from `GET /v1/orders/` with Pay (pending→funded), Cancel (→cancelled), service title resolution via cached `GET /v1/services/:id`. Executor dashboard: My Services CRUD (create/edit/delete via modal, `GET /v1/services/my`), Incoming Orders from `GET /v1/orders/sold` with Complete (funded→released) and Cancel. Loading/empty/error states handled. XSS-safe inline onclick via index-based array. | `curl localhost:8000/dashboard/client` (200), `localhost:8000/dashboard/executor` (200), `.../dashboard.js` (200) | Service title in orders requires individual `GET /v1/services/:id` calls (N+1). Future: add `service_title` to OrderRead schema. |
| 2026-05-30 | Remove Browse Services button from landing page | `templates/index.html` | Removed redundant "Browse Services" button from landing page to simplify UI | Visual check | — |
| 2026-05-30 | Logout redirects to homepage instead of /login | `static/js/auth.js` | `handleLogout` now redirects to `/` (homepage) instead of `/login` so users see the landing page after logout | Logout → redirects to `/` (200) | — |
| 2026-05-30 | Navbar links right-aligned, footer sticks to bottom | `static/css/main.css`, `templates/base.html` | Navbar: `margin-left: auto` on `.navbar-menu` pushes links to right edge. Footer: `body { display: flex; flex-direction: column; min-height: 100vh; } main { flex: 1; }` ensures footer stays at bottom even on short pages | Visual check on short (login) and long (catalog) pages | — |
| 2026-05-31 | Phase 7.3 rework — escrow proxy, dashboard CSS, full state-machine UI | `api/v1/escrow_proxy.py`, `app/services/escrow_client.py`, `api/v1/__init__.py`, `static/css/dashboard.css`, `static/js/dashboard.js`, `templates/dashboard_client.html`, `templates/dashboard_executor.html`, `templates/base.html` | Created escrow proxy router (`/v1/escrow/:order_id/:action`) that tries Go escrow first, falls back to direct order status PATCH when Go unavailable. Added `advance_escrow()` and `complete_escrow()` to EscrowClient. Created `dashboard.css` with 7 escrow state badges, skeleton shimmer animation, escrow timeline, responsive table. Rewrote `dashboard.js` with full escrow state-machine (fund→advance→complete→release), debounce on action clicks, optimistic UI updates via `updateOrderStatusUI()`. Rewrote `dashboard_client.html` with loading/empty/error/skeleton states for each escrow state. Rewrote `dashboard_executor.html` with tabs (My Services / Incoming Orders), service CRUD modal, escrow-aware action buttons. Cache busting via `?v=2` on all CSS/JS to force browser refresh. Added try-catch wrappers and null-checks throughout dashboard JS. | `cd services/python-api && python -m pytest tests/ -q` (32/32 passed), `curl localhost:8000/dashboard/client` (200), `curl localhost:8000/dashboard/executor` (200), `curl localhost:8000/api/static/css/dashboard.css` (200), `curl localhost:8000/v1/escrow/{id}/fund` (201 or clean 503 fallback), `python -c "from api.v1.escrow_proxy import router"` (clean import) | In-memory `order_id→escrow_id` cache (lost on restart, sufficient for dev). Go escrow host unreachable in dev mode → proxy returns 503, JS falls back to PATCH. 4 atomic commits in `step_7` branch. |
| 2026-05-31 | Phase 7.4 — Escrow panel, order detail, order modal | `api/v1/chain_proxy.py`, `static/css/escrow.css`, `static/css/audit.css`, `templates/order-modal.html`, `static/js/order-modal.js`, `templates/order_detail.html`, `static/js/escrow-panel.js`, `templates/audit.html`, `static/js/audit.js` | Backend: chain_proxy.py — blockchain audit proxy with mock fallback when blockchain-sim unavailable; registered in api/v1/__init__.py. CSS: escrow.css (timeline steps, action buttons by role/status, info grid, skeleton), audit.css (block chain layout, integrity badges, copy-hash button, responsive). Order modal: order-modal.html (description textarea, date picker, file upload, offer checkbox) + order-modal.js (validation, spinner, 422 error display, redirect to /dashboard/client?tab=orders). Blockchain audit page: audit.html (skeleton→empty→error→chain blocks) + audit.js (renders block chain, verifies prev_hash integrity, copy hash via clipboard API). Order detail page: order_detail.html (info grid + escrow panel container + audit link) + escrow-panel.js (5-step timeline CREATED→FUNDED→IN_PROGRESS→COMPLETED→RELEASED, role-aware action buttons with X-Idempotency-Key, optimistic UI without reload, fallback to PATCH). | `curl localhost:8000/orders/{id}` (200), `curl localhost:8000/audit/{order_id}` (200), `curl .../chain_proxy.py` (clean import), `python -m pytest tests/ -q` (32/32) | Go escrow host unreachable in dev → proxy returns clean 503, JS falls back to PATCH order status. Mock blockchain data returned when blockchain-sim unavailable. |
| 2026-05-31 | Phase 7.4 — Dashboard refactor, api-client, shared utils | `static/js/api-client.js`, `static/js/dashboard.js`, `static/js/utils.js`, `static/js/api.js`, `templates/base.html` | api-client.js: centralized fetch wrapper with X-Request-ID, retry on 5xx/429 (2 attempts, exponential backoff). dashboard.js: clientActions()/executorActions() replaced inline escrow buttons with "Details" link → /orders/{id}. utils.js: formatDate, statusBadge, formatPrice moved here (shared across all pages). api.js: added X-Request-ID to all outgoing requests. base.html: linked escrow.css, audit.css, api-client.js. Cache bust updated to v=4 on all Phase 7.4 assets. | `curl .../static/js/api-client.js` (200), `curl .../static/js/utils.js` (200) | All pages now use shared utils. api-client.js wraps all API calls with retry. |
| 2026-05-31 | Phase 7.4 refinement — Marketplace card redesign + DB seed | `static/css/catalog.css`, `static/js/catalog.js`, `templates/catalog.html`, `seed_products.sql` | catalog.css: full rewrite — 3-column grid, card with photo 4:3 (picsum.photos/seed/{id}/400/300), category badge, discount badge, rating stars + score + reviews, seller name, stacked price (original strike-through above current), "Add to Cart" button, hover lift + shadow + scale(1.05). catalog.js: getMockProductData() generates deterministic mock data (15 categories, 10 sellers, rating 2.5–5.0, 10–990 reviews, ~25% with 5–25% discount), renderStars(), card click → modal (ignores buttons via closest()), stopPropagation on Add to Cart, results count display. catalog.html: title "Products", search placeholder "Search products...", 4 skeleton cards. Database: deleted 31 old IT services, inserted 30 marketplace products (electronics, clothing, home, sports, accessories) with prices $14.99–$149.99 via raw sqlite3. | `sqlite3 marketplace.db "SELECT COUNT(*) FROM services"` (30), `curl localhost:8000/catalog` (200, product cards with images) | Picsum.photos requires internet for images. Deterministic mock data from service ID hash — no schema changes. |
| 2026-05-31 | Phase 7.5: Финализация UI, UX-компоненты и стабильность | `static/js/toast.js`, `static/css/skeleton.css`, `static/css/responsive.css`, `static/js/api-client.js`, `static/js/order-modal.js`, `static/js/dashboard.js`, `static/js/utils.js`, `static/js/catalog.js`, `templates/base.html`, `templates/order-modal.html`, `templates/catalog.html`, `templates/dashboard_client.html`, `templates/dashboard_executor.html`, `templates/audit.html`, `static/css/catalog.css`, `static/css/main.css` | Toast-система (success/error/warning/info, SVG-иконки, ARIA-live, 4s auto-hide), skeleton.css (единый shimmer, консолидация из catalog.css/dashboard.css), responsive.css (адаптивность таблиц → card-вид на 768px/480px, touch-targets 44×44px, prefers-reduced-motion, skip-link, focus-visible), focus trap в order-modal и service-modal (Tab/Shift+Tab цикл, Escape закрывает, return focus), ARIA-атрибуты во всех шаблонах (role="dialog", aria-modal, aria-label, aria-describedby, aria-expanded, scope="col", role="alert", role="status"), api-client.js (timeout 8s через AbortController, exponential backoff 1s→2s→4s, 3 попытки), глобальные error boundaries (window.onerror + unhandledrejection → toast + console), double-click protection (disable на Save/Submit), sidebar aria-expanded toggle, обновление cache bust до v=5 | `cd services/python-api && python -m pytest tests/ -v` (32/32 pass), `curl localhost:8000/` (200), `curl .../static/js/toast.js` (200), `curl .../static/css/skeleton.css` (200), `curl .../static/css/responsive.css` (200), `curl localhost:8000/catalog` (200), `curl localhost:8000/login` (200) | Проверить focus trap в order-modal / service-modal (Tab/Shift+Tab не уходит из модалки); Escape закрывает. Скелетоны должны исчезать после загрузки. Toast 4 типа (success/error/warning/info) c ARIA-live. Responsive: Chrome DevTools 768px → таблицы в cards, 480px → 1 колонка. Touch-targets min 44×44px во всех кнопках. |
| 2026-05-31 | Phase 7.6 — Wallet module (бэкенд + фронтенд) | `models/wallet.py`, `schemas/wallet.py`, `repositories/wallets.py`, `api/v1/wallet.py`, `api/v1/ui.py`, `static/js/wallet.js`, `static/js/auth.js`, `static/css/wallet.css`, `templates/wallet-card.html`, `templates/wallet.html`, `templates/dashboard_client.html`, `templates/base.html`, `alembic/versions/8b57a29864d5_add_wallet.py`, `tests/test_wallet.py`, `requirements.txt`, `Dockerfile`, `tests/conftest.py` | Wallet model (Wallet + Transaction), Pydantic схемы, WalletRepository/TransactionRepository, API: GET /balance, POST /topup, GET /transactions, 10 тестов. Фронтенд: карточка баланса, topup-модалка, история транзакций, отдельная страница /wallet, навигация (Wallet link в шапке). Docker: PostgreSQL вместо SQLite, chown appuser. | `pytest tests/test_wallet.py -v` (10/10 pass), `curl .../wallet/balance`, `curl .../wallet/topup`, `curl .../wallet/transactions`, `curl localhost:8000/wallet` (200) | Cache bust: auth.js?v=6, wallet.js?v=3. История операций убрана с dashboard, только на /wallet. |
| 2026-05-31 | Bug fix — race condition в WalletRepository.get_or_create | `repositories/wallets.py` | При параллельных запросах balance + transactions два запроса видели wallet=None → оба INSERT → UniqueViolationError. Исправлено: try/except IntegrityError с rollback() + повторный SELECT. | `pytest tests/test_wallet.py -v` (10/10) | — |
| 2026-05-31 | Bug fix — orders.status VARCHAR(9)→VARCHAR(11) | `alembic/versions/825161ca6ac3_fix_orders_status_enum.py` | Миграция расширяет orders.status с VARCHAR(9) до VARCHAR(11) — добавляет статусы in_progress (11 символов) и completed (9) которые не влезали в 9. | `alembic upgrade head` | Обратимая миграция. Использует ALTER COLUMN TYPE вместо sa.Enum для совместимости. |
| 2026-06-01 | Migration — SQLite → PostgreSQL (users + services + orders) | `services/python-api/marketplace.db` (SQLite) | Перенесены 16 новых пользователей (обновлены 2 пересекающихся), 30 сервисов, 1 заказ из SQLite в PostgreSQL. Удалены существующие товары в PG перед миграцией. | `curl login test7@gmail.com/12345678` (OK), `GET /v1/services/` (30 items) | Один заказ пропущен (orphaned — ссылался на несуществующий service). |
| 2026-06-01 | Wallet/pay endpoint + frontend integration (escrow fund) | `api/v1/wallet.py`, `schemas/wallet.py`, `api/v1/orders.py`, `static/js/api.js`, `static/js/dashboard.js`, `static/js/escrow-panel.js`, `static/js/utils.js`, `core/exceptions.py` | Создан `POST /v1/wallet/pay` — списание с кошелька, создание escrow_fund транзакции, обновление заказа на `funded`. Обновлён фронтенд: `doAction('fund')` и `handleEscrowPanelAction('fund')` вызывают `apiWalletPay` вместо старого escrow proxy. `showModalAlert()` для ошибки insufficient balance. Валидация 422 с полями выводится в модалке заказа. | `curl POST /v1/wallet/pay` (201), `curl GET /v1/wallet/balance` (баланс уменьшен), `curl GET /v1/orders/{id}` (status=funded) | Go-escrow не вызывается при fund — деньги только в Python БД. |
| 2026-06-01 | Infrastructure fixes (Docker, DB_URL, .dockerignore, schema) | `.env`, `.env.local`, `.dockerignore`, `core/config.py`, `go-escrow migrations`, `go-escrow repo` | Исправлено: `DB_URL` удалён из `.env` (мешал Docker Compose на Windows), `.env.local` для локального PostgreSQL, `config.py` грузит `[".env", ".env.local"]`, `.dockerignore` исключает `.env.local`, go-escrow таблица `transactions`→`escrow_transactions` (коллизия с Python моделью). | `docker compose up -d` (5/5 healthy), `docker compose down && up` (данные сохраняются) | — |
| 2026-06-01 | Bug fix — page reload on escrow button click | `static/js/escrow-panel.js`, `templates/base.html`, `templates/order_detail.html` | Добавлен `type="button"` ко всем `<button>` в `renderEscrowActions()`, `#confirm-modal` и Retry. Без этого `<button>` по умолчанию `type="submit"` отправляет форму и перезагружает страницу. | `Ctrl+Shift+R`, клик по кнопкам escrow — без перезагрузки | — |
| 2026-06-01 | Bug fix — status not updating after escrow action | `static/js/escrow-panel.js` | Заменён `await loadEscrowPanel(orderId)` (повторный GET с возможным кэшем) на `updateEscrowPanelUI(orderId, mapPanelActionToStatus(action))` — обновляет UI напрямую без запроса к серверу. | Клик Fund → бейдж `funded`, таймлайн шаг 2 активен | — |
| 2026-06-01 | Audit — escrow system has fundamental gaps | — | **Выявлено:** деньги списываются с покупателя при `fund` но не доходят до продавца при `release`. Нет `transfer()` в WalletRepository. Go-escrow получает `amount="0"` и не держит реальные средства. Ожидает команды `НАЧАТЬ` для исправления. | — | Требуется: escrow holding account, payout продавцу при release, transfer между кошельками. |
| 2026-06-01 | Phase 7.7 — Escrow money flow (transfer + payout + refund) | `models/wallet.py`, `repositories/wallets.py`, `api/v1/wallet.py`, `api/v1/escrow_proxy.py`, `api/v1/orders.py`, `README_AI.md`, `docs/ai-usage-log.md` | **WalletRepository.transfer()** — атомарный перевод между кошельками (дебет+кредит+2 транзакции). **Escrow holding account** — системный `escrow@marketplace.local` (ленивое создание). **Fund** (`POST /v1/wallet/pay`): buyer → escrow holding. **Release** (`POST /v1/escrow/{id}/release` + fallback `PATCH /orders/{id}/status`): escrow → seller (payout). **Cancel** (`PATCH /orders/{id}/status`): escrow → buyer (refund). В `TransactionType` добавлен `transfer`. | `python -c "import ast; ast.parse(...)"` — 5 файлов OK | Убедиться, что escrow holding создаётся при первом fund (лениво), а не при старте. Проверить E2E: register → topup → order → fund → advance → complete → release (баланс продавца увеличился). |
| 2026-06-01 | Phase 7.7 — Escrow money tests (12 tests, 11 written) + bugfixes + frontend initWallet | `repositories/wallets.py`, `tests/test_escrow_money.py`, `static/js/escrow-panel.js`, `static/js/dashboard.js`, `templates/order_detail.html`, `templates/dashboard_client.html`, `templates/dashboard_executor.html`, `README_AI.md`, `docs/ai-usage-log.md` | **ESCROW_USER_ID** fixed from `00000000-...-0001`→`1133d650-...-903a4` (SQLite stores small-int UUID as integer 1, breaking UUID decoding). **Test suite**: 11 tests for escrow money (fund, cancel refund, release payout, insufficient balance, auth). Fixed `test_pay_success` — filtered by `escrow_fund` type to ignore topup txn. **Frontend**: `initWallet()` called after `release`/`cancel` in both `escrow-panel.js` and `dashboard.js` so wallet balance updates immediately. All 53/53 tests pass. | `python -m pytest tests/ -v` (53/53 passed) | Проверить, что при release/cancel на dashboard баланс обновляется без перезагрузки страницы. |
| 2026-06-01 | Phase 7.7 — Wallet card on executor dashboard + initWallet role fix + fallback | `templates/dashboard_executor.html`, `static/js/wallet.js`, `templates/wallet.html`, `templates/dashboard_client.html` | **Карточка баланса** добавлена на дашборд исполнителя (`wallet-card.html` + `wallet.js` + `initWallet()`). **initWallet()** — убрано ограничение `auth.role !== 'client'`, теперь баланс загружается для любой роли. **renderWalletBalanceFallback()** — при ошибке API скелетон скрывается (показывается `0 ₽` + тост), а не висит вечно. **Защищённый вызов** `if (typeof initWallet === 'function')` во всех шаблонах. Cache-bust wallet.js обновлён до v=4. | 53/53 passed | — |