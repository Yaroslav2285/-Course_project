| Дата/Время | Задача/Промпт | Файлы | Результат | Верификация | Ручные правки |
|---|---|---|---|---|---|
| 2026-05-26 | Этап 1: структура монорепозитория и инфраструктура | `docker-compose.yml`, `.env.example`, `Makefile`, `.pre-commit-config.yaml`, `.gitignore`, `.editorconfig`, `.gitattributes`, `docs/README.md`, `services/*/.gitkeep`, `infra/.gitkeep`, `scripts/.gitkeep` | Созданы базовая структура и инфраструктурные файлы | `docker compose config && make up && docker ps` и `pre-commit run --all-files` | Убедиться, что `Makefile` и `docker-compose.yml` подходят для следующего этапа с Dockerfile-ами и сервисами |
| 2026-05-26 | Этап 2: модели данных и миграции для Python и Go | `services/python-api/models/users.py`, `services/python-api/models/services.py`, `services/python-api/models/orders.py`, `services/python-api/models/base.py`, `services/python-api/alembic/env.py`, `services/python-api/alembic.ini`, `services/go-escrow/internal/db/migrations/001_initial.sql` | Сгенерированы SQLAlchemy модели, Alembic async конфигурация и Go SQL миграция | `cd services/python-api && alembic revision --autogenerate -m "init" && alembic upgrade head` и `cd services/go-escrow && goose create init sql && goose up` | Проверить, что `alembic.ini` и `env.py` корректно настроены и миграции создаются без ошибок |
| 2026-05-26 | Исправление Alembic конфигурации и добавление requirements.txt | `services/python-api/alembic/env.py`, `services/python-api/requirements.txt`, `docs/ai-usage-log.md` | Добавлено чтение DB_URL из окружения для Alembic и зафиксированы Python зависимости для этапа миграций | `cd services/python-api && python -m pip install -r requirements.txt && python -m alembic -c alembic.ini revision --autogenerate -m "init"` | Проверить, что `DB_URL` доступен в окружении и Alembic запускается из каталога сервиса |
| 2026-05-26 | Исправление Alembic секции в alembic.ini | `services/python-api/alembic.ini`, `docs/ai-usage-log.md` | Перенесён `sqlalchemy.url` в секцию `[alembic]`, чтобы Alembic корректно читал URL | `cd services/python-api && python -m alembic -c alembic.ini revision --autogenerate -m "init"` | Проверить, что Alembic запускается из каталога `services/python-api` и `DB_URL` установлен при необходимости |
| 2026-05-26 | Добавление шаблона Alembic script.py.mako | `services/python-api/alembic/script.py.mako`, `docs/ai-usage-log.md` | Восстановлен отсутствующий шаблон для генерации ревизий Alembic | `cd services/python-api && python -m alembic -c alembic.ini revision --autogenerate -m "init" && python -m alembic -c alembic.ini upgrade head` | Проверить, что `alembic/versions` создаёт файлы ревизий и что `upgrade head` проходит |
| 2026-05-26 | Исправление миграции: DROP TABLE IF EXISTS и удаление неиспользуемого импорта | `services/python-api/alembic/versions/fff36bd858b0_init.py`, `services/python-api/models/base.py`, `docs/ai-usage-log.md` | Заменён `DROP TABLE goose_db_version` на `DROP TABLE IF EXISTS goose_db_version` (падал на свежей БД); удалён неиспользуемый `import enum` из `base.py` | `docker run -d --name pg_test -e POSTGRES_DB=app_db -e POSTGRES_USER=app_user -e POSTGRES_PASSWORD=ChangeMe123! -p 5432:5432 postgres:15-alpine && cd services/python-api && alembic upgrade head && alembic check` | Проверить, что `alembic upgrade head` проходит на чистой БД без ошибок и `alembic check` выводит `No new upgrade operations detected` |
| 2026-05-26 | Этап 3: Python Core (FastAPI, JWT, CRUD, Pydantic V2) | `core/config.py`, `core/db.py`, `core/security.py`, `core/exceptions.py`, `core/deps.py`, `core/responses.py`, `schemas/common.py`, `schemas/users.py`, `schemas/services.py`, `schemas/orders.py`, `repositories/base.py`, `repositories/users.py`, `repositories/services.py`, `repositories/orders.py`, `api/v1/auth.py`, `api/v1/users.py`, `api/v1/services.py`, `api/v1/orders.py`, `main.py`, `Dockerfile`, `requirements.txt`, `tests/` (5 файлов) | 32 файла: JWT auth, 4 CRUD эндпоинта с пагинацией, `condecimal` для цен, единый формат ответа, X-Request-ID, CORS, structlog, 23 теста (91% покрытие) | `cd services/python-api && ruff check . && pytest tests/ -v --cov=.` | Проверить, что Dockerfile собирается (`docker build -t python-api ./services/python-api`), тесты проходят на PostgreSQL (через `docker compose up -d postgres`) |
| 2026-05-26 | Этап 3.5: Web UI + SQLite migration + bugfixes | `core/config.py`, `core/db.py`, `core/exceptions.py`, `main.py`, `api/v1/ui.py`, `templates/base.html`, `templates/index.html`, `templates/login.html`, `templates/dashboard.html`, `templates/escrow_status.html`, `static/css/style.css`, `static/js/app.js`, `requirements.txt`, `tests/conftest.py` | 14 файлов: HTML5/CSS3/JS frontend (5 Jinja2 шаблонов), SQLite вместо PostgreSQL, исправлен TemplateResponse, get_db commit, static 404 через url_for, тесты под SQLite | `ruff check . && python -m pytest -v` | Убедиться, что все 23 теста проходят, сервер стартует на SQLite без .env, статика отдаётся через /api/static/ |
| 2026-05-27 | Этап 4: Go Escrow Microservice (Gin, state-machine, NUMERIC(19,4), rate-limit, idempotency) | `main.go`, `internal/config/config.go`, `internal/db/db.go`, `internal/domain/escrow.go`, `internal/repository/escrow_repo.go`, `internal/service/escrow_service.go`, `internal/api/middleware.go`, `internal/api/idempotency.go`, `internal/api/handler.go`, `internal/api/router.go`, `Dockerfile`, `Makefile`, `.env.example`, `go.mod`, `internal/domain/escrow_test.go`, `internal/service/escrow_service_test.go`, `internal/api/handler_test.go` | 17 файлов: Gin REST API (POST /v1/escrow, /fund, /release, /dispute, GET /{id}, /health), State-machine с 7 статусами и картой валидных переходов, X-Idempotency-Key (in-memory + TTL + goroutine cleanup), token-bucket rate-limiter на /fund, zap JSON logging, graceful shutdown, 47 table-driven тестов | `cd services/go-escrow && go build ./... && gosec ./... && go test ./... -coverprofile=cover.out` | Убедиться, что gosec установлен (0 issues), проверить запуск через docker compose, создать интеграционные тесты для полного escrow-цикла |
| 2026-05-27 | Исправление Этапа 2: Makefile, seed_db.py, healthcheck curl | `Makefile`, `scripts/seed_db.py`, `services/python-api/Dockerfile`, `.dockerignore` | Makefile: удалён дубликат `own:`, test запускает pytest + go test; seed_db.py: полный цикл регистрации(клиент+провайдер) → услуга → публикация → заказ; Dockerfile: добавлен curl для healthcheck; .dockerignore: исключён .env из сборки | `make test-python && make test-go`, `python scripts/seed_db.py` | Проверить `Dockerfile` — curl установлен; `.dockerignore` — .env не копируется |
| 2026-05-27 | Ручная проверка Этапа 3 (Python API через Docker) | — | Проверены: регистрация, логин, users/me, services CRUD, orders CRUD, статусы заказов, error-коды (401/404/409). Все эндпоинты работают с PostgreSQL | `curl http://localhost:8000/health`, `curl -X POST ... /v1/auth/register`, `curl -X POST ... /v1/orders/` | Обратить внимание: services — PUT (не PATCH), users/me — PUT, orders статусы — строгий enum, services листинг — только published по умолчанию |
| 2026-05-27 | Исправление ошибок в Docker-сборке Python API | `requirements.txt`, `main.py`, `services/python-api/Dockerfile` | Добавлен email-validator (ошибка pydantic); убрано условие `if _is_sqlite` для create_all — таблицы создаются для любого движка; добавлен curl в образ для healthcheck | `docker compose up -d --build python-api && docker ps` | Проверить что контейнер healthy, health эндпоинт отвечает |
| 2026-05-27 | Исправление DB_URL в Docker-контейнере | `services/python-api/.dockerignore` | Создан .dockerignore для исключения .env из Docker-образа. Старый контейнер использовал SQLite из .env вместо PostgreSQL из docker-compose | `docker compose up -d --force-recreate python-api && docker exec marketplace_python_api env \| Select-String DB_URL` | Убедиться что DB_URL = `postgresql+asyncpg://...`, а не `sqlite+...` |
| 2026-05-27 | Ручная проверка Этапа 4 (Go escrow через curl) | — | Проверены: POST /v1/escrow (201), /fund (200), /release (200), /dispute (200), GET /{id} (200), invalid transition (409), idempotency (409 cached), rate-limit (429), health (200), invalid UUID (400), not found (404) | `curl http://localhost:8081/health`, `curl -X POST http://localhost:8081/v1/escrow/ ...` | Проверить, что go-escrow не конфликтует с python-api на одном порту |
| 2026-05-27 | Комплексная проверка Этапов 2-4 | — | Подтверждена работоспособность всех этапов: Stage 2 (таблицы в PG, Alembic), Stage 3 (23/23 тестов, Docker healthy), Stage 4 (47/47 тестов) | `pytest -q`, `go test ./...`, `docker ps`, `alembic upgrade head` | — |
| 2026-05-27 | Этап 5: Blockchain Simulator (аудит-трейл, SHA-256, SQLite) | `services/blockchain-sim/requirements.txt`, `services/blockchain-sim/Dockerfile`, `services/blockchain-sim/config.py`, `services/blockchain-sim/schemas.py`, `services/blockchain-sim/blockchain.py`, `services/blockchain-sim/database.py`, `services/blockchain-sim/main.py`, `services/blockchain-sim/tests/__init__.py`, `services/blockchain-sim/tests/conftest.py`, `services/blockchain-sim/tests/test_blockchain.py` | 10 файлов: FastAPI сервис на порту 8082, Block/Blockchain с SHA-256, SQLite persistence, 4 эндпоинта (POST /v1/chain/submit, GET /v1/chain/verify, GET /v1/chain/audit/{order_id}, GET /health), 10 тестов (99% покрытие), structlog, Pydantic V2 валидация, prepared statements | `cd services/blockchain-sim && ruff check . && black --check . && pytest tests/ -v --cov=. && bandit -r .` | Проверить интеграцию: Go escrow вызывает POST /v1/chain/submit после каждого изменения статуса. Проверить docker compose build для blockchain-sim. |